---
phase: "01-async-foundation-and-app-factory"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/rag/vector_store.py
  - backend/.env.example
  - backend/pytest.ini
autonomous: true
requirements:
  - CORE-03
  - OPS-07

must_haves:
  truths:
    - "No module-level settings value binding exists in vector_store.py — EMBEDDING_MODEL_NAME constant is deleted"
    - "All Settings fields have a corresponding entry in backend/.env.example including ALLOWED_ORIGINS"
    - "pytest discovers and runs tests from backend/ directory without import errors"
    - "No .env file is tracked in git"
  artifacts:
    - path: "backend/app/rag/vector_store.py"
      provides: "CORE-03 fix — settings value read inside function body"
      contains: "def create_embedding_model"
    - path: "backend/app/core/config.py"
      provides: "allowed_origins field on Settings"
      contains: "allowed_origins"
    - path: "backend/.env.example"
      provides: "Complete settings reference for onboarding"
      contains: "ALLOWED_ORIGINS"
    - path: "backend/pytest.ini"
      provides: "pytest runner configuration"
      contains: "[pytest]"
  key_links:
    - from: "backend/app/core/config.py"
      to: "backend/app/rag/vector_store.py"
      via: "settings.embedding_model_name read at call time"
      pattern: "settings\\.embedding_model_name"
---

<objective>
Fix the CORE-03 anti-pattern (module-level settings binding in vector_store.py), add the
`allowed_origins` field to Settings so CORS origins are not hardcoded in source, update
`.env.example` to cover all Settings fields for onboarding, and create `pytest.ini` so the
test runner discovers tests correctly from the `backend/` directory.

Purpose: This plan delivers the prerequisite configuration and settings-hygiene fixes that
Plan 02 builds on. It is safe to execute in Wave 1 because no other plan depends on it and it
touches only config/infra files — no route or pipeline logic.

Output: Updated config.py (+1 field), vector_store.py (constant removed), .env.example
(complete), and new pytest.ini.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-async-foundation-and-app-factory/01-RESEARCH.md
@.planning/phases/01-async-foundation-and-app-factory/01-PATTERNS.md
@.planning/phases/01-async-foundation-and-app-factory/SKELETON.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix CORE-03 — remove module-level EMBEDDING_MODEL_NAME binding in vector_store.py</name>
  <files>backend/app/rag/vector_store.py</files>
  <read_first>
    - backend/app/rag/vector_store.py (current state — see the EMBEDDING_MODEL_NAME constant at line 8)
    - backend/app/scripts/build_index.py (verify no external import of EMBEDDING_MODEL_NAME before deleting)
    - .planning/phases/01-async-foundation-and-app-factory/01-PATTERNS.md (Pattern for vector_store.py, "Target delta" section)
  </read_first>
  <action>
    Before modifying, run: grep -rn "EMBEDDING_MODEL_NAME" backend/ to confirm no external importer
    uses this constant. Per RESEARCH.md Pitfall 4, if any file imports EMBEDDING_MODEL_NAME from
    vector_store.py, provide a compatibility shim before removing it. If no external importer is
    found (expected per research), proceed.

    In backend/app/rag/vector_store.py:
    - Delete line 8: EMBEDDING_MODEL_NAME = settings.embedding_model_name
    - Update create_embedding_model() to read settings.embedding_model_name directly inside the
      function body: return HuggingFaceEmbeddings(model_name=settings.embedding_model_name)
    - All other functions (build_faiss_vector_store, save_faiss_vector_store,
      load_faiss_vector_store) remain unchanged in signature and behavior.
    - The settings import at line 5 is already present — do not add a second import.
  </action>
  <verify>
    <automated>grep -n "^EMBEDDING_MODEL_NAME" backend/app/rag/vector_store.py | wc -l</automated>
    Expected output: 0 (the constant no longer exists at module level)
    <automated>grep -n "settings.embedding_model_name" backend/app/rag/vector_store.py</automated>
    Expected: line inside create_embedding_model() body containing settings.embedding_model_name
  </verify>
  <done>
    - vector_store.py contains no top-level EMBEDDING_MODEL_NAME constant assignment
    - create_embedding_model() reads settings.embedding_model_name inside the function body
    - All other functions in vector_store.py are unchanged
    - grep -rn "from app.rag.vector_store import EMBEDDING_MODEL_NAME" returns no matches
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add allowed_origins to Settings and complete .env.example</name>
  <files>backend/app/core/config.py, backend/.env.example</files>
  <read_first>
    - backend/app/core/config.py (current state — add field after enable_query_rewrite at line 20)
    - backend/.env.example (current state — 6 lines, missing most Settings fields)
    - .planning/phases/01-async-foundation-and-app-factory/01-PATTERNS.md (config.py "Target delta" and .env.example "Missing keys to add" sections)
  </read_first>
  <action>
    In backend/app/core/config.py:
    - Add one field to the Settings class after enable_query_rewrite (line 20):
        allowed_origins: list[str] = ["http://localhost:5173", "http://127.0.0.1:5173"]
    - Match the existing field declaration style: snake_case name, type annotation, inline default.
    - The settings = Settings() singleton at line 36 is correct at module level — do not move it.
    - No other changes to config.py.

    In backend/.env.example:
    - Preserve the 6 existing lines exactly as-is (do not alter key names or existing values).
    - Append the missing fields in logical groups with inline comment section headers.
    - Add (in this order after the existing content):

      # Embeddings / retrieval
      EMBEDDING_MODEL_NAME=sentence-transformers/multi-qa-MiniLM-L6-cos-v1
      RERANKER_MODEL_NAME=cross-encoder/ms-marco-MiniLM-L6-v2
      RETRIEVAL_CANDIDATE_K=15
      RERANKED_TOP_K=5

      # Paths (relative to backend dir, or absolute)
      FAISS_INDEX_PATH=../data/indexes/faiss_student_manual
      PDF_PATH=../data/raw/student_manual_2019.pdf

      # CORS
      ALLOWED_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

    - Use UPPER_SNAKE_CASE=value format matching the existing entries.
    - Do not add a real secret or real key value — all values remain placeholders or safe defaults.

    Secrets hygiene check: run git ls-files | grep -iE "\.env$" — confirm output is empty (no
    .env files tracked). If any .env is tracked, run git rm --cached on it and confirm it is
    in .gitignore. Document the result in the task notes.
  </action>
  <verify>
    <automated>grep -n "allowed_origins" backend/app/core/config.py</automated>
    Expected: a line inside the Settings class body containing allowed_origins: list[str] =
    <automated>grep -n "ALLOWED_ORIGINS" backend/.env.example</automated>
    Expected: one line containing ALLOWED_ORIGINS=
    <automated>grep -c "EMBEDDING_MODEL_NAME\|RERANKER_MODEL_NAME\|RETRIEVAL_CANDIDATE_K\|RERANKED_TOP_K\|FAISS_INDEX_PATH\|PDF_PATH\|ALLOWED_ORIGINS" backend/.env.example</automated>
    Expected output: 7
  </verify>
  <done>
    - Settings class in config.py has allowed_origins: list[str] field with default list
    - .env.example contains entries for EMBEDDING_MODEL_NAME, RERANKER_MODEL_NAME,
      RETRIEVAL_CANDIDATE_K, RERANKED_TOP_K, FAISS_INDEX_PATH, PDF_PATH, ALLOWED_ORIGINS
    - git ls-files | grep -iE "\.env$" returns no output (OPS-07 verified)
    - No real API key or secret value is present in any tracked file
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create pytest.ini for test discovery from backend/</name>
  <files>backend/pytest.ini</files>
  <read_first>
    - .planning/phases/01-async-foundation-and-app-factory/01-PATTERNS.md (pytest.ini "No analog found" section — standard pattern)
    - .planning/phases/01-async-foundation-and-app-factory/01-RESEARCH.md (Validation Architecture section — test framework table)
  </read_first>
  <action>
    Create backend/pytest.ini with exactly these contents:

    [pytest]
    testpaths = tests
    pythonpath = .

    Rationale: testpaths = tests points pytest at backend/tests/. pythonpath = . adds
    backend/ to sys.path so all test files can do "from app.*" imports without error.
    This is the pytest >= 7 standard pattern. No other keys are needed for Phase 1.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_anthropic_provider.py tests/test_gemini_provider.py tests/test_query_rewrite.py -x -q 2>&1 | tail -5</automated>
    Expected: all tests pass or skip (no import errors, no collection errors).
    Note: test_query_rewrite.py tests that patch create_llm_provider will still pass at this
    point because query_transformer.py still has the old signature — that is intentional.
    The signature changes happen in Plan 02.
  </verify>
  <done>
    - backend/pytest.ini exists with [pytest] section containing testpaths = tests and pythonpath = .
    - Running "python -m pytest tests/ -x -q" from backend/ directory collects and runs all
      existing tests without import errors or collection failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env file → Settings | .env values flow into Settings fields at process startup; no external trust boundary crossed in this plan |
| .env.example → git | .env.example is committed; must contain only placeholders, never real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | backend/.env.example | mitigate | All values are placeholders; git ls-files audit in Task 2 confirms no real .env is tracked |
| T-01-01-02 | Information Disclosure | backend/.gitignore | accept | .env already gitignored (verified pre-plan); OPS-07 audit in Task 2 re-confirms |
| T-01-01-SC | Tampering | No package installs in this plan | accept | No npm/pip/cargo installs — package legitimacy gate not applicable |
</threat_model>

<verification>
After all three tasks complete, run from the backend/ directory:

1. python -m pytest tests/ -x -q
   Expected: all tests pass (or skip if real API keys absent — provider tests may skip)

2. grep -n "^EMBEDDING_MODEL_NAME" backend/app/rag/vector_store.py | wc -l
   Expected: 0

3. grep -c "allowed_origins" backend/app/core/config.py
   Expected: at least 1

4. git ls-files | grep -iE "\.env$"
   Expected: no output (OPS-07 satisfied)
</verification>

<success_criteria>
- EMBEDDING_MODEL_NAME module-level constant is deleted from vector_store.py; settings.embedding_model_name is read inside create_embedding_model() body
- Settings class has allowed_origins: list[str] field; ALLOWED_ORIGINS appears in .env.example
- backend/pytest.ini exists; "python -m pytest tests/ -x -q" from backend/ exits 0
- git ls-files shows no .env files tracked
- All currently-passing tests still pass after these changes
</success_criteria>

<output>
Create .planning/phases/01-async-foundation-and-app-factory/01-01-SUMMARY.md when done
</output>
