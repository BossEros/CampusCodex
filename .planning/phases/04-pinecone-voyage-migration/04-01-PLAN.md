---
phase: "04-pinecone-voyage-migration"
plan: "01"
type: plan
status: proposed
depends_on:
  - "01-async-foundation-and-app-factory"
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04
  - RAG-05
  - RAG-06
  - RAG-07
  - EVAL-01
---

# Pinecone + Voyage Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the local FAISS and sentence-transformers retrieval stack with a production-ready managed retrieval stack using Pinecone for vectors and Voyage for embeddings and reranking, while preserving cited answers and keeping the project aligned with the existing roadmap.

**Architecture:** Keep the current layered RAG pipeline and existing provider pattern. Introduce a real retrieval seam first, then swap concrete adapters behind it. Preserve the two-stage Retrieval Pipeline (`Retrieval -> Reranking`) and keep `benchmark` isolated from `shared_kb` at the storage layer.

**Tech Stack:** FastAPI, Pinecone serverless, Voyage API, Pydantic settings, existing `LlmProvider` factory pattern, pytest/unittest, LangChain document types where still useful.

---

## Summary

This plan is intentionally **migration-first**. It does not try to implement the full admin ingestion product in one pass. The objective is to make the retrieval substrate production-ready first, then leave the codebase in the right shape for the later document-management phase.

The work is split into slow, trackable phases so the team can implement and verify one production concern at a time:

1. Establish a retrieval seam so the app is no longer typed directly against `FAISS`
2. Add embedding and reranker provider abstractions that mirror the existing LLM provider pattern
3. Introduce Pinecone as the managed vector adapter and switch runtime retrieval to it
4. Rebuild the benchmark corpus into the correct Pinecone namespace and verify source-cited answers still work
5. Remove heavy local ML dependencies and update docs/ops guidance

---

## Phase 0: Lock Scope and Baseline

**Purpose:** Freeze the migration target and make sure the team is not mixing Phase 4 work with later ingestion/admin features.

- [x] Confirm this plan only covers the retrieval migration and benchmark setup, not admin upload/list/delete UI
- [x] Treat the current single-PDF `student_manual` flow as the canonical migration baseline
- [x] Record the target runtime shape:
  - Pinecone serverless index
  - `dimension=1024`
  - `metric=cosine`
  - `shared_kb` namespace for app retrieval
  - `benchmark` namespace for eval-only retrieval
  - Voyage embeddings via `voyage-3.5`
  - Voyage reranking via `rerank-2.5`
- [x] Record the core non-negotiables:
  - Preserve answer citations with page metadata
  - Preserve the two-stage Retrieval Pipeline
  - Remove `faiss-cpu`, `sentence-transformers`, and `torch` from the backend image by the end
  - Do not mix benchmark vectors with future admin-uploaded vectors

**Deliverable:** Phase 0 is complete. The locked baseline lives in `04-00-BASELINE.md`, and this plan is now the source of truth for Phase 4 execution.

---

## Phase 1: Create the Retrieval Seam

**Purpose:** Remove direct FAISS coupling before introducing Pinecone.

**Primary files**
- `backend/app/rag/chat_service.py`
- `backend/app/rag/vector_store.py`
- `backend/app/main.py`
- `backend/tests/test_query_rewrite.py`

- [x] Define a small retrieval interface for the runtime path instead of passing a raw `FAISS` object through the app
- [x] Rename or refactor `vector_store.py` so it represents a vector-store module, not a FAISS-only helper
- [x] Replace FAISS-specific typing in `chat_service.py` with the new interface
- [x] Keep the retrieval method shape intentionally small:
  - `search_similar_chunks(query: str, limit: int) -> list[tuple[Document, float]]`
  - Add only the methods needed in this phase
- [x] Keep the current FAISS-backed adapter temporarily so regression tests can still pass while Pinecone is being introduced
- [x] Update app startup so `app.state.vector_store` stores the adapter, not a FAISS object

**Acceptance criteria**
- [x] No request path in runtime code imports or types directly against `FAISS`
- [x] The existing chat flow still works with the temporary FAISS adapter
- [x] `chat_service.py` depends on the seam, not on a vendor implementation

**Status note:** Complete. Runtime retrieval now goes through a `VectorStore` seam, `load_faiss_vector_store()` returns a FAISS-backed adapter instead of a raw FAISS object, `chat_service.py` calls `search_similar_chunks(...)`, and `main.py` stores the adapter on `app.state.vector_store`.

---

## Phase 2: Add Provider Abstractions for Embeddings and Reranking

**Purpose:** Match the existing architecture instead of scattering Voyage calls across the codebase.

**Primary files**
- `backend/app/embeddings/`
- `backend/app/reranking/`
- `backend/app/core/config.py`
- `backend/app/rag/reranker.py`

- [x] Create an `EmbeddingProvider` protocol plus a factory
- [x] Create a `RerankerProvider` protocol plus a factory
- [x] Add Voyage-backed adapters for both provider families
- [x] Extend settings with the minimum production config needed:
  - Pinecone API key
  - Pinecone index name
  - Voyage API key
  - Voyage embedding model name
  - Voyage reranker model name
  - Pinecone namespace names
- [x] Keep provider creation centralized in startup/factory code, consistent with the existing `LlmProvider` pattern
- [x] Wrap blocking provider SDK calls with `asyncio.to_thread` where they are invoked from async request or ingestion paths
- [x] Preserve reranking behavior and top-k semantics from the current implementation

**Acceptance criteria**
- [x] The codebase has three aligned provider families: LLM, Embedding, and Reranker
- [x] No Voyage SDK usage is embedded directly inside routes
- [x] The reranking stage is still explicit and testable

**Status note:** Complete. The repo now has `embeddings/` and `reranking/` provider families that mirror the LLM pattern, `rag/reranker.py` routes through `create_reranker_provider()`, Voyage adapters exist for both capabilities, and the required Pinecone/Voyage settings are defined. The actual vector-store runtime cutover to Pinecone remains Phase 3.

---

## Phase 3: Introduce Pinecone as the Runtime Vector Adapter

**Purpose:** Switch retrieval from local FAISS to managed Pinecone without breaking the rest of the pipeline.

**Primary files**
- `backend/app/rag/vector_store.py`
- `backend/app/main.py`
- `backend/app/core/config.py`
- `backend/tests/`

- [ ] Implement a Pinecone-backed vector-store adapter behind the new seam
- [ ] Initialize the Pinecone client once during startup and make it available through app state or adapter construction
- [ ] Implement namespace-aware query behavior so runtime chat reads from `shared_kb`
- [ ] Preserve metadata needed for source attribution:
  - document title
  - page number
  - source identifier/path if still needed internally
  - chunk text
- [ ] Map Pinecone results into the same document-and-score shape expected by the retrieval and reranking pipeline
- [ ] Keep FAISS only as a temporary local fallback during migration execution, then remove it once Pinecone is verified

**Acceptance criteria**
- [ ] `/api/chat` is served from Pinecone-backed retrieval
- [ ] Source citations still include title and page information
- [ ] Query results still flow through reranking before answer generation

---

## Phase 4: Rebuild the Benchmark Corpus and Verify Behavior

**Purpose:** Re-embed the canonical student manual into the correct Pinecone namespace and prove the migrated pipeline still works.

**Primary files**
- `backend/app/scripts/build_index.py`
- `backend/app/rag/pdf_loader.py`
- `backend/app/rag/text_chunker.py`
- `README.md`
- `DEVELOPER_GUIDE.md`

- [ ] Replace the FAISS-oriented build script with a benchmark-seeding script for Pinecone
- [ ] Reuse the existing PDF loader and chunker instead of rewriting ingestion logic
- [ ] Seed the `student_manual` corpus into the `benchmark` namespace using Voyage embeddings
- [ ] Decide and document whether the same script also seeds `shared_kb` for local/demo startup, or whether that remains a separate later-step concern
- [ ] Verify that benchmark seeding preserves metadata required for source citations
- [ ] Run end-to-end checks against representative questions:
  - direct factual question
  - vague question that relies on query rewriting
  - follow-up question with history
  - no-answer / weak-context question

**Acceptance criteria**
- [ ] `benchmark` is populated from the canonical student manual
- [ ] Eval and app retrieval do not share a namespace
- [ ] Representative chat scenarios still produce cited answers after migration

---

## Phase 5: Remove Heavy Local Dependencies and Clean Up Docs

**Purpose:** Finish the production-readiness slice instead of leaving a half-migrated stack.

**Primary files**
- `backend/requirements.txt`
- `README.md`
- `DEVELOPER_GUIDE.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`

- [ ] Remove `faiss-cpu`, `sentence-transformers`, and `torch` from backend dependencies
- [ ] Remove FAISS-specific language from setup docs and developer docs
- [ ] Replace “build local FAISS index” instructions with “seed Pinecone namespace” instructions
- [ ] Document the new required environment variables and namespace expectations
- [ ] Update roadmap/progress notes so the team can clearly see what Phase 4 changes and what remains deferred to Phase 6
- [ ] Explicitly document the migration boundary:
  - Phase 4 ends with production-ready retrieval
  - Phase 6 adds admin upload/list/delete/re-index and async document ingestion

**Acceptance criteria**
- [ ] Backend dependency list reflects the slim production stack
- [ ] Team docs no longer describe FAISS as the runtime storage layer
- [ ] The repo clearly distinguishes completed migration work from deferred ingestion work

---

## Test Plan

### Unit and service tests

- [ ] Add tests for the retrieval seam so runtime code can be verified without a real vendor backend
- [ ] Add tests for `EmbeddingProvider` and `RerankerProvider` factory selection
- [ ] Add tests that Pinecone result metadata is transformed into correct source output
- [ ] Update existing query-rewrite and chat-service tests to use the new seam instead of FAISS mocks

### Integration and regression checks

- [ ] Verify app startup initializes the production retrieval adapter successfully when env vars are present
- [ ] Verify `/api/index/status` reports Pinecone-oriented runtime information instead of FAISS-path details
- [ ] Verify `/api/chat` still returns:
  - answer text
  - sources
  - correct page numbers where metadata exists
- [ ] Verify empty or missing retrieval results still produce the current graceful fallback answer

### Manual verification

- [ ] Seed the benchmark namespace
- [ ] Ask at least 5 representative student-manual questions
- [ ] Confirm citations remain believable and page-linked
- [ ] Confirm no local FAISS artifacts are required for startup

---

## Explicit Assumptions and Defaults

- This plan is **migration-first**, not the full document-management implementation
- The plan lives inside `.planning` because this repo already uses that as the team tracking system
- `shared_kb` is the runtime namespace and `benchmark` is the eval-only namespace
- The existing single-document corpus is the only corpus migrated in this phase
- Pinecone is the production adapter; FAISS is allowed only as a temporary migration scaffold and must be removed by the end of the phase
- Voyage is the default provider for embeddings and reranking because the roadmap already locks that direction
- Admin upload/list/delete/re-index flows remain out of scope for this file and should be implemented in the later multi-document phase

---

## Recommended Execution Order

- [ ] Execute Phase 1 first and do not skip it
- [ ] Do not implement Pinecone directly inside route handlers or `chat_service.py`
- [ ] Do not remove FAISS until Pinecone-backed retrieval is proven end-to-end
- [ ] Do not start admin ingestion work until this migration slice is green

## Follow-on After This Plan

Once this plan is complete, the next recommended markdown should be a separate execution plan for:

- Phase 6 multi-document ingestion and admin management
- document status lifecycle
- document deletion cleanup against `shared_kb`
- re-index flows
- upload safeguards and backoff/retry behavior
